{
  "name": "savora-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node backend/server.js",
    "server": "node backend/server.js",
    "build": "cd frontend && npm install && npm run build"
  }
}
